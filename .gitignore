# Local key material — contains real API keys + webhook secrets, never commit (SPEC §13)
/config/keys.yaml

# Build output
/bin/
# stray top-level binaries from `go build` without `-o`
/*.exe
/api
/worker
/observer
/loadtest
/migrate
/keygen

# Local environment overrides
.env
.claude/

# scripts/local-setup.* runtime state
/.shortlink-pids
/.shortlink-containers
/logs/

# Autogenerated by cmd/limits render -- regenerated every setup run.
/deploy/docker-compose.override.yml
/deploy/k8s/values-local.yaml
# OS / editor cruft
.DS_Store
Thumbs.db
